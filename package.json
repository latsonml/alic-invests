{
  "name": "alic-invests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "dev:api": "node --env-file=.env.local scripts/local-api.mjs",
    "build": "vite build && cp dist/alic-income-fund.html dist/index.html",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
